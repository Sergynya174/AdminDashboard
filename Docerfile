# Используйте базовый образ node для сборки вашего React проекта

FROM node:14.17.1

# Установите рабочую директорию внутри контейнера

WORKDIR /app

# Скопируйте файлы package.json и package-lock.json внутрь контейнера

COPY package\*.json ./

# Установите зависимости

RUN npm install

# Скопируйте все файлы проекта внутрь контейнера

COPY . .

# Соберите React проект

RUN npm run build

# Установите сервер для обслуживания статических файлов

RUN npm install -g serve

# Определите команду для запуска сервера

CMD serve -s build

# Определите порт, который будет прослушиваться внутри контейнера

EXPOSE 3000
